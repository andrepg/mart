name: smartcado
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  sqflite_common_ffi:
  path:
  cupertino_icons:
  firebase_core:
  animations:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  test:

flutter:
  uses-material-design: true